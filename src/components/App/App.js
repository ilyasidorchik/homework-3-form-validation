import React from 'react';
import Form from 'components/Form';

const App = (props) => {
    return (
        <div className="app-container">
            <Form/>
        </div>
    );
};

export default App;